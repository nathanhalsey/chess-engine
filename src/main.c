#include"headers.h"
int main(int argc, char const *argv[])
{
    init_attack_vectors();
    for (int i = 0; i < 64; i++){
        //print_board(pawn_)
    }
    //print_board(pawn_attacks(H4,black));
    return 0;
}